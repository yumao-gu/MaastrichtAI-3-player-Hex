#include <iostream>
#include "./include/HEX.h"
#include <math.h>
#include "./include/BOARD.h"
#include "./include/PLAYER.h"
#include "include/GRAPH.h"

int main() {

/*    Board board(1);
//    board.Print();
    PLAYER playerA(A,A_side);
    PLAYER playerB(B,B_side);
    PLAYER playerC(C,C_side);

//    playerB.Move(Hex(1,1,-2),board);

    if(playerC.CanWin(board))
    {
        cout<<"there is still a C path"<<endl;
    } else{
        cout<<"C is blocked"<<endl;
    }

    Graph graph1,graph2;

//    Line a1 = Line(A,graph1._nodes.front(),Hex(0,0,0));
//    Node b1(B,&a1);
//    a1.SetChild(b1);
//    graph1._nodes.front()->InsertChildren(a1);
//    graph1._nodes.push_back(&b1);

//    graph1.AddNode(A, graph1._nodes.front(), Hex(0, 0, 0), B);
//    graph1.AddNode(A, graph1._nodes.front(), Hex(1, 0, -1), B);
//    graph1.AddNode(A, graph1._nodes.front(), Hex(-1, 1, 0), B);

////    cout<<"graph2 size : " << graph2.Size()<<endl;
//    cout<<"graph1 size : " << graph1.Size()<<endl;
////    cout<<"graph1 children size : " << graph1._nodes.front()->_children.size()<<endl;
////    cout<<"graph1._nodes.front()->_children.front() address : "<< graph1._nodes.front()->_children.front()<<endl;
////    cout<<"graph1._nodes.front()->_children hex: " << graph1._nodes.front()->_children.front()->_q<<'\t'
////        << graph1._nodes.front()->_children.front()->_r<<'\t'<< graph1._nodes.front()->_children.front()->_s<<'\t'
////            << graph1._nodes.front()->_children.front()->_player<<endl;
//
//    graph2 = graph1;
//
//    for(auto it = graph2._nodes.front()->_children.begin(); it != graph2._nodes.front()->_children.end();it++)
//    {
//        cout<<(*it)->_q<<'\t'<<(*it)->_r<<'\t'<<(*it)->_s<<'\t'<<endl;
//    }*/


    std::cout << "Hello, World!" << std::endl;
    return 0;
}
